<?xml version="1.0" encoding="utf-8"?>
<!-- 用来自定义购物车界面的 支付 按钮 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">


    <corners android:radius="25dp"></corners>
    <stroke android:width="0dp"></stroke>
    <gradient
        android:startColor="#6AD3EB"
        android:endColor="#2395B2"
        android:angle="0"
        android:type="linear"></gradient>
    <!--android:startColor="#FF7F02"  渐变起始色值
    android:centerColor=""      渐变中间色值
    android:endColor="#FF600B"    渐变结束颜色
    android:angle="0"      渐变的方向 默认为0 从左向右 ，90时从下向上 必须为45的整数倍
    android:type="radial"       渐变类型 有三种 线性linear 放射渐变radial 扫描线性渐变sweep
    android:centerX="0.5"       渐变中心相对X坐标只有渐变类型为放射渐变时有效
    android:centerY="0.5"       渐变中心相对Y坐标只有渐变类型为放射渐变时有效
    android:gradientRadius="100"    渐变半径 非线性放射有效-->

</shape>